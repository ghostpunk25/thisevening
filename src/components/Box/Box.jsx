import styled from "styled-components";
import { color, space, typography, shadow, layout, flexbox, background, position, grid, border } from 'styled-system';

export const Box = styled('div')(color, shadow, space, typography, layout, flexbox, background, position, grid, border)

